post_install() {
	[ -f /etc/openldap/ldap.conf ] || cp /etc/openldap/ldap.conf.default /etc/openldap/ldap.conf
	/sbin/ldconfig -m /usr/lib
}

pre_remove(){
	if cmp -s /etc/openldap/ldap.conf /etc/openldap/ldap.conf.default; then 
		rm -f /etc/openldap/ldap.conf; 
	fi
	/sbin/ldconfig -R
}

