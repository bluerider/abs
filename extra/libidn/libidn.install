info_dir=/usr/local/share/info

post_install() {
    install-info ${info_dir}/libidn.info.gz ${info_dir}/dir 2> /dev/null
}

post_upgrade() {
        post_install
}

pre_remove() {
    install-info --delete ${info_dir}/libidn.info.gz ${info_dir}/dir 2> /dev/null
}
